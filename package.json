{
  "name": "hw-eight",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:sass": "sass public/scss/styles.scss public/css/styles.css -w",
    "firebase:serve": "firebase serve --only hosting",
    "firebase:deploy": "firebase deploy --except functions"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.56.0"
  },
  "dependencies": {
    "firebase-tools": "^11.16.1"
  }
}
